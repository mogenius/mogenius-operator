---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: permissions.mogenius.com
spec:
  group: mogenius.com
  names:
    kind: Permission
    listKind: PermissionList
    plural: permissions
    singular: permission
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Grant permissions to a workspace for a group
            properties:
              delete:
                description: |-
                  grant "delete" permission

                  this allows deleting the workspace itself
                type: boolean
              group:
                description: the group for which this permission is created
                type: string
              read:
                description: |-
                  grant "read" permission

                  this allows both viewing the workspace itself and reading all resources within this workspace
                type: boolean
              workspace:
                description: the workspace to which this permission is applied
                type: string
              write:
                description: |-
                  grant "write" permission

                  this allows editing the workspace which means:
                  - users are allowed to create, change and delete resources within this workspace
                type: boolean
            type: object
          status:
            type: object
        type: object
    served: true
    storage: true
