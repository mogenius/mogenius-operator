---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: platformpatches.mogenius.com
spec:
  group: mogenius.com
  names:
    kind: PlatformPatch
    listKind: PlatformPatchList
    plural: platformpatches
    singular: platformpatch
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A mogenius `PlatformPatch` defines patches that can be applied
          to the configuration of a platform component.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Grant permissions to a workspace for a group
            properties:
              extraObjects:
                description: extra Kubernetes objects to be applied to the cluster.
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              valuesObject:
                description: helm values to be merged into the configuration of a
                  platform component.
                x-kubernetes-preserve-unknown-fields: true
            type: object
          status:
            type: object
        required:
        - metadata
        - spec
        - status
        type: object
    served: true
    storage: true
