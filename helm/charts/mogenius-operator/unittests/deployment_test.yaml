suite: deployment
templates:
  - deployment.yaml
tests:
  - it: renders with default values
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: mogenius-operator
      - equal:
          path: spec.replicas
          value: 1
      - equal:
          path: spec.template.spec.containers[0].name
          value: mogenius-operator

  - it: uses chart appVersion as image tag when tag is not set
    asserts:
      - matchRegex:
          path: spec.template.spec.containers[0].image
          pattern: "ghcr.io/mogenius/mogenius-operator:.+"

  - it: uses custom image tag when set
    set:
      image.tag: v1.2.3
    asserts:
      - equal:
          path: spec.template.spec.containers[0].image
          value: ghcr.io/mogenius/mogenius-operator:v1.2.3

  - it: sets revisionHistoryLimit from values
    set:
      revisionHistoryLimit: 5
    asserts:
      - equal:
          path: spec.revisionHistoryLimit
          value: 5

  - it: includes valkey initContainer when valkey is enabled
    set:
      valkey.enabled: true
    asserts:
      - isNotEmpty:
          path: spec.template.spec.initContainers

  - it: omits initContainers when valkey and debugTools are disabled
    set:
      valkey.enabled: false
      features.debugTools.enabled: false
    asserts:
      - isNull:
          path: spec.template.spec.initContainers

  - it: sets containerSecurityContext
    asserts:
      - equal:
          path: spec.template.spec.containers[0].securityContext.privileged
          value: false
      - equal:
          path: spec.template.spec.containers[0].securityContext.readOnlyRootFilesystem
          value: true

  - it: renders probes when enabled
    set:
      probes.enabled: true
    asserts:
      - isNotEmpty:
          path: spec.template.spec.containers[0].livenessProbe
      - isNotEmpty:
          path: spec.template.spec.containers[0].readinessProbe
      - isNotEmpty:
          path: spec.template.spec.containers[0].startupProbe

  - it: omits probes when disabled
    set:
      probes.enabled: false
    asserts:
      - isNull:
          path: spec.template.spec.containers[0].livenessProbe
      - isNull:
          path: spec.template.spec.containers[0].readinessProbe
      - isNull:
          path: spec.template.spec.containers[0].startupProbe

  - it: adds podLabels to pod template
    set:
      podLabels:
        team: platform
        env: prod
    asserts:
      - equal:
          path: spec.template.metadata.labels.team
          value: platform
      - equal:
          path: spec.template.metadata.labels.env
          value: prod
